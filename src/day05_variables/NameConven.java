package day05_variables;

public class NameConven {

    public static void main(String[] args) {

        // int public = 5; // we can NOT use the same words each we already have
        // boolean class = 7; // the same , we can NOT use 'class' because we already have it
        // we cannot use : class , public , byte, any reserved words - goto
        // Variable name can contain: Letters: any lower or capital case letters; Numbers: any number from 0-9;
        //~Special Character: Only TWO special character can be used --->$ and _
        //~ No Space: canNot have a space in a variableName

    }
}
